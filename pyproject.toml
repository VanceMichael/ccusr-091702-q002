[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nutrition-evidence"
version = "0.1.0"
description = "营养宣称证据台账后端服务"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["app", "migrate"]